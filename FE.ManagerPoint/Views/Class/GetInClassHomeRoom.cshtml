@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.Title = "Lớp học";
    var classId = ViewBag.idClass;
    var classInfo = ViewBag.ClassInfor;
}
<div class="row p-3">
    <div id="classInfo" class="card col-md-12" style="width: 100%;"></div>
</div>
<div class="row mt-3">
    <div class="nav nav-tabs" id="nav-tab" role="tablist">
        <button class="nav-link active" id="nav-1-tab" data-bs-toggle="tab" data-bs-target="#nav-1" type="button" role="tab" aria-controls="nav-1" aria-selected="false">Kì 1</button>
        <button class="nav-link" id="nav-2-tab" data-bs-toggle="tab" data-bs-target="#nav-2" type="button" role="tab" aria-controls="nav-2" aria-selected="true">Kì 2</button>
        <button class="nav-link" id="nav-3-tab" data-bs-toggle="tab" data-bs-target="#nav-3" type="button" role="tab" aria-controls="nav-3" aria-selected="true">Cả năm</button>
    </div>
    <div class="tab-content" id="nav-tabContent">
        <div class="tab-pane fade show active" id="nav-1" role="tabpanel" aria-labelledby="nav-1-tab">
            <table id="GradePointStudent-1" class="table table-striped table-hover"></table>
        </div>
        <div class="tab-pane fade" id="nav-2" role="tabpanel" aria-labelledby="nav-2-tab">

            <table id="GradePointStudent-2" class="table table-striped table-hover"></table>
        </div>
        <div class="tab-pane fade" id="nav-3" role="tabpanel" aria-labelledby="nav-3-tab">
            <table id="GradePointStudent-3" class="table table-striped table-hover"></table>
        </div>
    </div>
</div>
@*<partial name="Update" />*@
<script src="~/js/GetInClassHomeRoom/GetInClassHomeRoom.js"></script>
<script>
    $(document).ready(function () {
        $('#semester').select2({ width: '100%' });
        let classId = '@classId';
        var classInfo = @Html.Raw(ViewBag.ClassInfor); // Lấy dữ liệu từ ViewBag
        console.log(classInfo);
        GetClassInfo(classInfo)
        GetDataGradePointS1(classId);
        GetDataGradePointS2(classId);
        GetDataGradePointS3(classId);
    })


    //$('#user_create_form').on('submit', function (event) {
    //    var form = $(this);
    //    if (!form[0].checkValidity()) {
    //        event.preventDefault();
    //        event.stopPropagation();
    //    } else {
    //        var formData = {};
    //        $('#user_create_form').find('input, select, textarea').each(function (index, element) {
    //            var fieldId = $(element).attr('id');
    //            var fieldType = $(element).attr('type');
    //            var fieldValue;
    //            if (fieldType === 'file') {
    //                return;
    //            }
    //            if ($(element).is('select') && $(element).attr('id') === 'gender') {
    //                fieldValue = parseInt($(element).val());
    //            } else {
    //                fieldValue = $(element).val();
    //            }

    //            formData[fieldId] = fieldValue;
    //        });
    //        console.log(formData);
    //        event.preventDefault();
    //        createUser(formData, function () {
    //            $('#user_create_form').trigger('reset');
    //            toastr.success('Tạo môn học thành công');
    //            $('#createModal').modal('hide');
    //            form.removeClass('was-validated');
    //        });
    //    }
    //    form.addClass('was-validated');
    //});
</script>